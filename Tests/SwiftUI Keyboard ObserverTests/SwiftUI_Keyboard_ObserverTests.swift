    import XCTest
    @testable import SwiftUI_Keyboard_Observer

    final class SwiftUI_Keyboard_ObserverTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(SwiftUI_Keyboard_Observer().text, "Hello, World!")
        }
    }
